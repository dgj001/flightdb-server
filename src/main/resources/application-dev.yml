spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flightdb_dev
    username: flightdb_dev_user
    password: ham22gonG
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: createtables.sql
